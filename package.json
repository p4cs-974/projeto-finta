{
  "name": "finta",
  "version": "1.0.0",
  "description": "FINancial Tracking & Analysis - Monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\""
  },
  "devDependencies": {
    "turbo": "^2.8.3",
    "prettier": "^3.0.0"
  },
  "packageManager": "bun@1.3.6",
  "dependencies": {
    "zod": "^4.3.6"
  }
}
